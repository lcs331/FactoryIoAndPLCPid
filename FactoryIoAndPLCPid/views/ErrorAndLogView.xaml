<UserControl x:Class="FactoryIoAndPLCPid.views.ErrorAndLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FactoryIoAndPLCPid.views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1250">
    <Grid Background="AliceBlue">
        <DataGrid Margin="20" ItemsSource="{Binding LogMessages}" AutoGenerateColumns="False" Height="680">
            <DataGrid.Columns>
                <DataGridTextColumn Header="时间" Binding="{Binding Time,StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="150" />
                <DataGridTextColumn Header="级别" Binding="{Binding Level}" Width="100" />
                <DataGridTextColumn Header="内容" Binding="{Binding Message}" Width="*" >
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            <!-- 超长省略号 -->
                            <Setter Property="TextWrapping" Value="NoWrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
